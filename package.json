{
  "name": "feedbackfruits-knowledge-search-broker",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "build-watch": "babel src --out-dir lib --watch",
    "prestart": "npm run build",
    "start": "node lib",
    "watch": "npm run build-watch & nodemon lib"
  },
  "author": "Joost Verdoorn",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^2.0.0",
    "elasticsearch": "^12.1.3",
    "memux": "^0.2.0",
    "p-queue": "^0.2.0",
    "rxjs": "^5.0.0-rc.4"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "nodemon": "^1.11.0"
  },
  "babel": {
    "presets": [
      "stage-0"
    ]
  }
}
