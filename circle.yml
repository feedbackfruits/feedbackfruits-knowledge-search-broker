machine:
  node:
    version: 6.0.0
test:
  override:
    - "true"
deployment:
  production:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - heroku container:push web -a fbf-knowledge-search-broker
  staging:
    branch: master
    heroku:
      appname: staging-fbf-knowledge-search-broke
